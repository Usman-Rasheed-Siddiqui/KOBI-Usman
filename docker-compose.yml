services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run start
    env_file: .env
    ports:
      - "3000:3000"
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run worker
    env_file: .env
